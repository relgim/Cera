# CERA Codex Manager Command 0060

Before every action, read:

1. `D:\AIChatBot\Cera\.chatgpt\pro-review\continuous-work\CURRENT.md`
2. the lexicographically highest `WORK_QUEUE_*.md`
3. every exact authority path named by `CURRENT.md`
4. `D:\CP25\source\.chatgpt\operations\self-continuation\LATEST.json`
5. `D:\AIChatBot\Cera\docs\authority\CERA_SEQUENCE_FIRST_IMPLEMENTATION_REFINEMENT_V1.md`
6. `D:\AIChatBot\Cera\docs\review\CERA_CLAUDE_SEQUENCE_FIRST_IMPLEMENTATION_REVIEW_REQUEST_2026-08-05.md`

Use `D:\CP25\source` as the execution repository.

## Goal

Correct and freeze the additive sequence-first implementation provider-free, publish its exact review commit to Git without force, and stop for independent Claude review. Do not make provider calls or start SillyTavern.

## Required correction

Preserve historical evidence and the clean normal DeepSeek Writer wire. Correct the new route so that:

- provider-facing semantic drafts contain no Python custody IDs, hashes, revisions, paths, or transactions;
- Python custody envelopes bind semantic outputs after decode;
- Python never infers presence or responders from names, regexes, aliases, prior active sets, references, or retrieval;
- accepted presence is preserved from accepted state and changes only through Validator-accepted ordered presence changes;
- Planner responders derive from NPC owners of intended items;
- backgrounded present NPCs are derived, never model-authored;
- absent/remote participation requires semantic authority and cannot be created by Python;
- Validator verdict is binary accept/reject, with orthogonal creator-review flags;
- beat coverage derives from realized-item Planner references;
- durable changes use approved opaque target keys;
- Writer retry eligibility derives from conflict class/controller failure type;
- affected/observing mini-ledgers are removed unless a demonstrated mechanical operation requires them;
- Planner stable instructions are one-time base context on a persistent branch thread;
- Validator is fresh per candidate with compact base instructions and no exhaustive fallback;
- Stage 6's new route does not use `_candidate_character_ids`, regex/name selection, default Sakura, prior active cast, `eligible_responder_ids`, or `planner_requested_character_ids` as semantic authority;
- retained TurnKernel/ingress/seed components are proven mechanical by tests.

Do not implement or activate the deferred adult capability route in this queue.

## Verification order

```text
exact contract tests
-> focused sequence-first tests
-> fake Planner/Writer/Validator/Reader pipeline
-> presence and branch adversarial fixtures
-> Stage 6 adapter/provider-free integration tests
-> source freeze
-> one complete repository suite
```

Do not run the full suite after each micro-edit.

Required adversarial fixtures include:

- merely mentioning absent Mia does not make her present or responsive;
- accepted-present Mia remains silent when she owns no Planner item;
- entry and exit apply in exact item order;
- ambiguous presence makes no state change;
- phone/text and off-screen speech do not imply physical presence;
- scene change re-establishes presence;
- no model output echoes custody IDs or hashes;
- no backgrounded list or duplicate coverage field exists;
- invalid target key cannot commit;
- rejected quote must exist in frozen prose;
- old exhaustive defaults cannot be selected silently by a new-route call site.

## Git publication

After all provider-free gates and the complete suite pass:

1. Materialize the current manager authority and Claude review request into the execution repository under the same repo-relative paths.
2. Review the exact diff.
3. Stage explicit coherent source, tests, and review documents only.
4. Create local commits as needed, with an accurate final checkpoint commit.
5. Fast-forward the existing review branch `review/cera-sequence-first-runtime-v1-20260805`, or create a duplicate-safe v2 branch if a safe fast-forward is impossible.
6. Push without force.
7. Verify the exact remote commit.
8. Write `D:\CP25\source\.chatgpt\operations\GIT_PUBLICATION_RESULT_Q0060.json` with branch, local and remote SHA, tree SHA, exact staged paths, tests, exclusions, and protected effects.
9. Write a final self-continuation result and notify the manager with the exact review branch and commit.
10. Stop for Claude review. Do not infer live-call authority.

## Provider and effect boundary

Provider calls: `0`.

Allowed: provider-free source/docs/tests, local commits, one non-force review-branch push, exact receipts.

Prohibited: live Codex/DeepSeek/Reader calls, SillyTavern qualification, 20-turn campaign, production mutation, active/default route promotion, installed-user SillyTavern change, adult-route activation, force push, merge, deployment, remote operation beyond the review push, destructive cleanup, history rewrite, credentials or secrets.
