# CERA Compact Reasoner V7 Checkpoint Request

checkpoint_id: `2026-07-31-compact-reasoner-v7-001`
status: progressions_1_3_complete_job4_pre_authorized

## Creator objective

Materially reduce ordinary-turn latency without weakening character logic,
causal continuation, current-scene cast selection, protected-user autonomy,
evidence/knowledge ownership, branch/regeneration isolation, creator review,
Python durable authority, or DeepSeek realization quality.

The exact frozen continuation is:

> Continue the scene with only the current characters as they talk to eachother.

The accepted branch head, not a hardcoded character rule, must identify Hana
and Mia as the active NPCs for this fixture.

## Fixed creator decisions

- Never use Codex Fast mode.
- Use `gpt-5.6-sol` at reasoning effort `medium`; do not test Sol-low.
- Do not change the independent verifier, DeepSeek model, or Composer behavior.
- Do not add retries, fallbacks, hidden repair, Detailer calls, or speculative
  provider calls.
- Do not activate compact v7 as the production/default route.
- Do not deploy, restart services, alter SillyTavern, bind production, or
  mutate a live story database.
- Provider conversation state is not durable story memory.
- Relevant accepted continuity cannot be removed merely to reduce tokens.
- No hard creative token ceiling.
- Progressions 1-3 use zero provider calls.
- Job 4 has exactly four authorized Sol-medium calls and stops afterward.

## Completed Progressions 1-3

1. Correct cumulative multi-step Codex timing/token telemetry while retaining
   no prompt, reasoning, output, tool arguments, private evidence, or secrets.
2. Add explicit current-scene scope, compact exact-evidence views, and a
   discardable branch/generation-bound reading capsule.
3. Add `CodexReasonerDraftV7Compact` behind explicit shadow/test-only selection
   and compile it deterministically through the unchanged v6/domain/Composer
   validation path.

## Concurrent Job 4 authorization

After the Progressions 1-3 commit and review publication, run exactly one
Sol-medium attempt for each of these variants against the same frozen source
and accepted state:

- A: broad packet + v6 + stored accepted ancestry;
- B: scoped compact packet + v6 + the same stored accepted ancestry;
- C: scoped compact packet + v7 + the same stored accepted ancestry;
- D: scoped compact packet + v7 + deterministic reconstructed context.

The calls run in an isolated Git worktree and disposable database. Check for
the identity-bound Pro response before and after each call. Stop before the next
provider call if Pro requests correction. Do not call DeepSeek or the verifier,
write story authority, retry, fall back, modify the active route, or self-
authorize Job 5.

## Review request

Independently inspect the bound source, tests, exact anatomy evidence, active-v6
hashes, and preservation boundaries. Identify any material logic loss,
telemetry misstatement, scene-scope error, alias/privacy/branch defect,
compiler-added semantic choice, invalid schema, or route-activation risk. The
review remains advisory and cannot expand creator authority.
