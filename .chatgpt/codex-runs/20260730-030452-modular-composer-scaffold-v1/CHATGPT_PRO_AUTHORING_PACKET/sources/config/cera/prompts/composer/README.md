# Modular Composer prompt source seam

This directory is intentionally inactive. It contains no `MANIFEST.json` and
no writing-facing module prose. The active Composer continues to use
`cera.deepseek_scene_composer_prompt.v18`.

ChatGPT Pro may author the future files listed in the frozen authoring packet.
Codex must then review their ownership, hashes, provider-schema compatibility,
duplication, and tests before any separately authorized integration or live
qualification.

Compiler v1 recognizes only `fixture_only` and `shadow_only`. It has no active
or provider-dispatch state.

