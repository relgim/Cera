# Decisions and Supersessions

**Status:** controlling decision register
**Rule:** older material remains provenance; it does not regain authority through reuse

## 1. Active decisions

| ID | Decision | Consequence |
|---|---|---|
| D-001 | `D:\AIChatBot\Cera` is the sole active repository. | No runtime import, path, or database dependency on Vera/V6 or E-drive material. |
| D-002 | Python is the deterministic authority and transaction owner. | Models propose; Python validates and commits. |
| D-003 | Core reasoning interface is `SceneReasonerPort`. | `CodexSceneReasonerPort` is an adapter, not a core dependency. |
| D-004 | Runtime Codex owns non-graphic causal logic and `SequencePlan`. | DeepSeek cannot silently re-decide character outcomes. |
| D-005 | DeepSeek Scene Composer owns complete visible prose. | No outline-first Core and no mandatory Detailer. |
| D-006 | Adult protected-mechanics enrichment is conditional and consent-valid only. | Refined by D-030; it is never used for blocked non-consensual events. |
| D-007 | Accepted prose is presentation-neutral and immutable. | UI rendering and summaries derive from it. |
| D-008 | No silent fallback or recursive retry. | Provider failure is explicit; state remains unchanged. |
| D-009 | Genesis is structured, versioned JSON authority with generated views. | Markdown indexes are not editable truth. |
| D-010 | Runtime Codex uses bounded retrieval tools. | Desktop filesystem access is not assumed at runtime. |
| D-011 | Memory separates events, knowledge, interpretation, relationship, material state, and development. | No monolithic prose memory and no automatic trait rewrite. |
| D-012 | Regenerations are immutable sibling candidates. | Earlier accepted artifacts remain recoverable; forks do not leak. |
| D-013 | Blocked events stop model generation at the boundary. | Only a validated non-graphic receipt can resume aftermath. |
| D-014 | Temporary aftermath projection is noncanonical. | It is isolated from story retrieval and deleted after reconciliation. |
| D-015 | External handlers are opaque and user-owned. | CERA documents only the neutral interface and receipt contract. |
| D-016 | Hana facts and knowledge boundaries are those in `CREATOR_FACTS_AND_PREFERENCES.md`. | Earlier trajectory/profile predictions are not canon. |
| D-017 | Examples are selected, conditional craft demonstrations. | They cannot activate content or establish character facts. |
| D-018 | Semantic abstraction preserves causal and authority meaning. | It may remove graphic wording, never intent, deliberateness, consent, discomfort, knowledge, action, or outcome. |
| D-019 | ChatGPT Instant has no default critical-path role. | Any use requires a bounded role and evidence of value. |
| D-020 | Derived-memory consolidation follows canonical publication. | Summaries and indexes cannot block or rewrite accepted prose. |
| D-021 | The initial sequential implementation authority covered Phases 1–7. | D-042 later extends the provider-free authority through Phase 8; phase gates and Pro review still apply. |
| D-022 | SQLite with WAL is the initial Phase 2 authority store. | Use foreign keys, migrations, optimistic heads, journals, idempotency, and recovery; no server database. |
| D-023 | Provider configuration may be inspected read-only, but live calls and secret copying/exposure are not authorized. | Provider adapters remain offline/fake until a later explicit live-call decision. |
| D-024 | Each completed phase receives a pinned ChatGPT Pro review. | Treat Pro's response as delegated in-scope creator guidance, but never as permission to override explicit hard limits. |
| D-025 | Every runtime evidence sequence is bound to one request-local immutable snapshot. | No mid-turn refresh, cross-perspective reuse, or budget bleed between separate requests. |
| D-026 | Phase 5 qualifies only a scripted fake `SceneReasonerPort`. | The fake proves contracts/orchestration, is rejected in production, and does not qualify runtime Codex. |
| D-027 | Phase 6 qualifies only a scripted fake `SceneComposerPort` plus structural Python acceptance. | It does not qualify DeepSeek or prove semantic/reader quality; the accepted artifact remains in memory and uncommitted. |
| D-028 | Composer source realization binds source-unit state as well as identity and order. | Attempted, ongoing, partial, interrupted, completed, and stopped cannot be silently changed while retaining the same source citation. |
| D-029 | An in-memory accepted artifact may reserve a deterministic transaction identity. | Reservation is not persistence; only a successful `CommitReceipt` establishes durable branch truth. |
| D-030 | The former “Adult Planner” role is narrowed to non-authoritative `AdultMechanicsPort` enrichment. | Scene Reasoner remains the sole psychological/decision owner; mechanics cannot override intent, tactic, cast, floor, uncertainty, consequences, or relationship meaning. |
| D-031 | Consent-valid adult routing uses synchronized dual representations. | Reasoner receives a non-graphic causal ledger; Composer alone receives the restricted exact envelope; receipts contain hashes and IDs, never protected text. |
| D-032 | Adult craft context is recomputed from typed current-turn authority. | Private suspicion, historical context, body state, examples, or provider conversation state cannot activate or leak adult families into another turn. |
| D-033 | Phase 7 qualifies only synthetic authority and scripted fake adult mechanics. | It does not qualify a provider, import Adult EX, establish real character authority, or prove adult prose quality. |
| D-034 | Phase 7's `actual_nonconsensual` classification means ineligible for adult realization, not erased history. | Current blocked events stop before all models; later continuity requires a separately authorized neutral receipt or non-graphic already-ended past-fact boundary and resumes aftermath only. |
| D-035 | Hanezawa Core Genesis V1.1 and its visual canon are the installed creator source revision. | The immutable source artifacts compile into the manifest-governed `hanezawa_core_v1_1` JSON package; production-world binding remains separately gated. |
| D-036 | Hana's husband is Mia's and Yuuni's biological father; Hana alone adopted Sakura, Enne, Tomi, and Aoi. | The conflicting V1.1 paternity unknown is excluded from active unknowns. The husband is not the four adopted daughters' adoptive father. |
| D-037 | Hana's husband is objectively cheating; Enne found evidence and Sakura verified enough to accept it. | Only Sakura and Enne know at story start. Hana's faithful conscious belief and subconscious unease remain separately typed and do not receive their knowledge. |
| D-038 | The creator Anima prompt overlay supersedes the source JSON's prompt prefix and negative-prompt composition. | Use the exact new positive prefix and exact supplied negative prompt; append no identity-drift negatives and do not add `loli`. |
| D-039 | Yuuni may look deliberately childish and youthful in ordinary nonsexual imagery while remaining canonically 18 and mentally capable. | Protected/adult imagery cannot reuse childlike or age-ambiguous cues and states age/status once without repetitive `adult` wording. |
| D-040 | Real-Genesis runtime integration may be calibrated only in auto-deleting development databases with fake adapters. | No caller-selected database path, live provider, Adult EX, production world/story data, Phase 8/9 expansion, SillyTavern, or deployment is implied. |
| D-041 | Evidence authorization is checked both when records are retrieved and when a character uses them. | Advertised fields must expand exactly, nested authoritative payload is searchable only as candidate text, exact fetch remains required, and `knowledge_owner_ids` constrain every character move, participation choice, and beat citation. |
| D-042 | The creator's 2026-07-28 instruction to continue authorizes Phase 8 inside the existing provider-free boundary. | Checkpoint, receipt, reconciliation, fake aftermath, restart, and atomic-resumption infrastructure may be implemented; external-handler internals, live providers, production data, Adult EX, SillyTavern, and deployment remain prohibited. |
| D-043 | The blocked-event correlation chain binds world, Genesis revision, protected-user identity, request/source, branch, generation, starting artifact, checkpoint, external request, registry version, and receipt hash. | Restart and later evidence creation do not depend on mutable ambient identity; a root branch is represented by paired null starting-artifact ID/hash. |
| D-044 | `CommitReceipt` v2 explicitly binds accepted external receipt IDs. | A resumption commit proves which validated external claim was promoted; story records, accepted prose, receipt state, branch head, and scratch-projection deletion share one transaction. |
| D-045 | Phase 8 aftermath state candidates are complete `EvidenceDocument` payloads. | Private trauma, relationship, material/reproductive, development, and objective-event records enter the same authorized search/fetch path as other branch evidence; a free-form memory blob is not accepted. |
| D-046 | The creator's 2026-07-28 instruction to continue authorizes provider-free Phase 9. | Deferred event-backed memory, relationship, thread, development, summary/index regeneration, supersession, replay, rollback, and branch-isolation infrastructure may be implemented; live providers, Adult EX, external-handler work, production data/world binding, SillyTavern, Phase 10, and deployment remain prohibited. |
| D-047 | Derived-authority concurrency uses a branch-local monotonic `authority_revision` independent of the accepted-story head and generation. | A consolidation can change durable memory without changing prose; every evidence snapshot and consolidation bundle must bind this revision so a concurrent consolidation becomes stale rather than silently overwriting derived truth. |
| D-048 | A fork inherits ancestor derived records only through the fork creation cutoff. | Derived records committed later to a parent at the same story artifact are not visible in an already-created child or its siblings. Branch ancestry alone is insufficient without the cutoff. |
| D-049 | Public, owner-private, and system-private derived summaries/indexes are regenerable views, never authority. | Canonical derived records remain the evidence source. A view failure is explicit and recoverable without undoing accepted prose or treating stale summary text as truth. |
| D-050 | Provider-free Phase 9 uses `DerivedConsolidatorPort` plus a production-prohibited scripted fake. | Fake proposals are valid only in synthetic worlds. Real disposable Genesis calibration uses validated-derived authority while still making zero provider or network calls. Live Codex consolidation remains unimplemented and separately gated. |
| D-051 | ChatGPT Pro returned advisory verdict `PHASE_9_ACCEPTED_PROVIDER_FREE`. | No in-scope correction was required from the evidence packet. This closes the required Phase 9 review gate but does not authorize Phase 10, live providers, production binding, Adult EX, external-handler work, SillyTavern, deployment, or Genesis promotion. |
| D-052 | The creator's 2026-07-28 instruction to continue authorizes Phase 10's provider-free evaluation foundation. | CERA may implement role-specific offline evaluation, holdout integrity, privacy-safe telemetry contracts, matched human-review packets, deterministic promotion recommendations, deployment-readiness gates, and disposable real-Genesis calibration. Live providers, Adult EX, external-handler work, production data/world binding, SillyTavern integration/publication, credentials, deployment, and final creator acceptance remain separately gated. |
| D-053 | ChatGPT Pro returned advisory verdict `PHASE_10_OFFLINE_EVALUATION_ACCEPTED`. | No in-scope correction was required. The verdict accepts only the provider-free evaluation foundation and does not qualify or authorize live providers, semantic evaluator correctness, Adult EX, handler work, credentials, production binding, SillyTavern, promotion, final creator acceptance, or deployment. |
| D-054 | The creator's 2026-07-28 instruction to "Go ahead" authorizes a bounded Phase 10 live-provider transport qualification milestone. | CERA may inspect current official provider facts, use already configured ChatGPT/DeepSeek authentication, implement provider transport contracts, and run non-story transport probes. Production worlds/data, Adult EX, external-handler work, SillyTavern, route promotion, and deployment remain closed. |
| D-055 | Current live candidates use Codex GPT-5.6 Sol/Terra/Luna and DeepSeek V4 Pro/Flash; old DeepSeek aliases are not CERA routes. | Sol-medium is the quality-first reasoner candidate, Terra-medium and Luna-medium are unpromoted comparators, V4 Pro is the quality-first Composer candidate, and V4 Flash is the comparator. GPT-5.5 is not retained as a live default merely for historical continuity; current Codex guidance identifies Terra as the natural starting replacement for prior 5.5 work. |
| D-056 | The initial Codex transport primitive is the Python Codex SDK `0.144.4` controlling its pinned local app-server runtime with ChatGPT-session auth. | Calls are ephemeral, run in an empty workspace under a no-files/no-network permission profile, use explicit model/effort and JSON Schema, execute in a killable subprocess, and retain no raw prompt/output in receipts. Desktop Codex is not the runtime backend. |
| D-057 | Phase 11 proves live transport availability and receipt/failure semantics only. | It does not implement the final `CodexSceneReasonerPort` evidence-tool bridge, qualify character judgment or DeepSeek prose, promote a route, bind production authority, or publish a story. Those claims require bounded MCP evidence retrieval, role-specific live suites, matched human review, and creator approval. |
| D-058 | ChatGPT Pro returned advisory verdict `PHASE_11_LIVE_PROVIDER_TRANSPORT_ACCEPTED`. | No in-scope correction was required. The verdict accepts transport connectivity, isolation, bounded execution, truthful identity/receipt semantics, and preserved failures only; it does not qualify a story role or authorize the MCP bridge, Adult EX, production binding, SillyTavern, promotion, deployment, or final creator acceptance. |
| D-059 | The creator's 2026-07-28 continuation instruction authorizes the bounded Phase 12 request-scoped Codex evidence-bridge milestone. | CERA may implement and test the MCP projection of existing typed evidence tools and run a synthetic non-story live tool probe. Story/Genesis/adult provider packets, production data/world binding, Adult EX, handler work, SillyTavern, promotion, and deployment remain closed. |
| D-060 | Runtime Codex evidence access is one authenticated loopback Streamable HTTP MCP server per reasoner request. | One immutable snapshot and cumulative budget back the current seven-operation allow-list, including bounded query-plan search. Global MCP servers are cleared, the ephemeral bearer is never receipted, Codex receives no filesystem/database API, and provider-observed tool calls must exactly reconcile with Python's bridge trace. |
| D-061 | ChatGPT Pro returned advisory verdict `PHASE_12_CODEX_MCP_EVIDENCE_BRIDGE_ACCEPTED`. | No in-scope correction was required. The verdict accepts bridge infrastructure and its bounded evidence only; it does not complete or qualify the typed Scene Reasoner, authorize story/Genesis/adult inputs, promote a route, activate DeepSeek, bind production, integrate SillyTavern, or deploy. |
| D-062 | The creator's standing continuation instruction authorizes provider-free Phase 13 typed `CodexSceneReasonerPort` integration after Phase 12 acceptance. | CERA may implement packet/schema/result mapping and deterministic tests against fake transport responses and the accepted bridge/validator. No live story-role, story/Genesis/adult provider packet, promotion, or production action is implied. |
| D-063 | `SceneReasonerPort` returns a transient provider-neutral adapter-call envelope; `SceneReasonerReceipt` v2 binds fake or live adapter evidence uniformly. | The coordinator retains Python validation ownership. A Codex execution must bind and retain one safe provider receipt plus one matching MCP bridge receipt; a fake execution must bind neither and report zero external calls. |
| D-064 | ChatGPT Pro returned advisory verdict `PHASE_13_CODEX_SCENE_REASONER_ADAPTER_ACCEPTED`. | No in-scope correction was required. The verdict accepts provider-free adapter mapping and Python validation reuse only; live schema acceptance, Codex judgment, route qualification, provider content calls, and every production gate remain unproven/closed. |
| D-065 | The creator's standing continuation instruction authorizes provider-free Phase 14 typed `DeepSeekSceneComposerPort` integration after Phase 13 acceptance. | CERA may implement bounded context/packet/schema/result mapping and deterministic tests against fake HTTP responses and the existing Python Composer validator. No live story/Genesis/adult provider call, route promotion, or production action is implied. |
| D-066 | DeepSeek receives a complete evidence-bound realization packet, not an action-only instruction, but remains the creative prose owner rather than an authority owner. | Each selected NPC requires bounded identity/voice evidence; craft is separately labeled and route-selected; DeepSeek supplies prose and quote-anchored declarations while Python owns IDs, hashes, offsets, decision fidelity, acceptance, persistence, and failure semantics. |
| D-067 | ChatGPT Pro returned advisory verdict `PHASE_14_DEEPSEEK_SCENE_COMPOSER_ADAPTER_ACCEPTED`. | No in-scope correction was required. The verdict accepts provider-free context/packet/result mapping and Python validation reuse only; live schema reliability, DeepSeek prose quality, end-to-end model judgment, route qualification, provider content calls, and every production gate remain unproven/closed. |
| D-068 | The creator's standing continuation instruction authorizes provider-free Phase 15 live-shaped turn integration after Phase 14 acceptance. | CERA may assemble bounded Composer context from real authorized evidence and exercise typed Codex/DeepSeek adapters with fake provider transports in disposable databases. No live content call, durable story commit, Adult EX, production binding, SillyTavern, promotion, or deployment is implied. |
| D-069 | Exact evidence used by the Reasoner remains available transiently to Python's post-decision context assembler. | It is selected only for validated active-character use and never enters normalized provider/bridge/turn receipts, telemetry, or durable state. Python deterministically adds one exact speech-system card per selected NPC; DeepSeek receives no retrieval tool. |
| D-070 | ChatGPT Pro returned advisory verdict `PHASE_15_LIVE_SHAPED_TURN_PIPELINE_ACCEPTED`. | No in-scope correction was required. The verdict accepts provider-free real-Genesis/fake-transport integration only; live model quality, durable publication, content calls, production gates, and creator acceptance remain unproven/closed. |
| D-071 | The creator's standing continuation instruction authorizes provider-free Phase 16 ordinary-turn transaction integration after Phase 15 acceptance. | CERA may publish validated ordinary fake-transport results only into disposable development databases. Adult publication, live content calls, production data/binding, Adult EX, SillyTavern, external-handler work, promotion, and deployment remain closed. |
| D-072 | Atomic turn publication persists receipt payload evidence, not only receipt IDs. | Migration 8 adds immutable content-addressed validation/lookup/provider receipt records owned by the same transaction as source, generation, artifact, branch head, and commit receipt. The ordinary builder supplies exact coverage; no prompt, raw evidence, prose, or secret enters these audit records. |
| D-073 | ChatGPT Pro returned advisory verdict `PHASE_16_TRANSACTIONAL_ORDINARY_TURN_ACCEPTED`. | No in-scope correction was required. The verdict accepts provider-free ordinary append/regeneration transaction integration only; adult publication, live content calls, semantic promotion, production binding, Adult EX, SillyTavern, external-handler work, and deployment remain closed. |
| D-074 | Every published ordinary turn atomically creates one system-private direct accepted-turn event record. | This supersedes Phase 16's zero-event publication choice. Deferred consolidation cannot invent objective events; it may derive memory/relationship/thread/development only from the exact direct event. The event contains validated current-segment structure and bindings, never derived psychology or future conditional segments. |
| D-075 | Rendering and derived consolidation are independent post-publication consumers. | Their failure cannot roll back accepted source/prose/event/generation truth. Replaying an already committed artifact reuses the durable consolidation receipt and does not call the consolidator or insert records again. |
| D-076 | Authoritative evidence insertion updates its FTS candidate projection inside the same transaction. | This prevents a committed story or consolidation record from making the next retrieval fail stale. FTS remains non-authoritative and branch/privacy filtering still occurs against canonical records. |
| D-077 | Consolidation receipt payloads are committed with derived authority, not represented only by IDs. | `cera.derived_consolidation_request.v2`, `cera.consolidation_bundle.v2`, and SQLite migration 9 carry lookup, provider-neutral consolidator, and validation receipt payloads into append-only transaction-owned audit records. |
| D-078 | ChatGPT Pro returned advisory verdict `PHASE_17_POST_PUBLICATION_ACCEPTED`. | No in-scope correction was required. The verdict accepts only the provider-free direct-event, atomic-search-projection, pure-rendering, and deferred-consolidation milestone; all live-content and production gates remain closed. |
| D-079 | Requested render, consolidation, and dependent derived-view work is scheduled atomically with accepted story publication. | `TurnCommitBundle` hash domain v2 and SQLite migration 10 prevent a committed artifact from losing its downstream work identity across process exit; story rollback removes the work rows too. |
| D-080 | Failed or interrupted post-publication work never retries implicitly. | Retry requires the exact failed work ID plus a non-empty authorization reason. Restart recovery changes ambiguous running work to sanitized review-required failure because external completion may be unknown. |
| D-081 | No-change consolidation and non-authoritative render/view results are durable operational evidence. | Replay reconstructs or links prior results without repeating the consolidator; no-change retains bounded lookup/provider/validation receipt evidence and prevents duplicate calls after restart. |
| D-082 | ChatGPT Pro returned advisory verdict `PHASE_18_DURABLE_POST_PUBLICATION_ACCEPTED`. | No in-scope correction was required. The verdict accepts only the provider-free durable post-publication scheduling/recovery boundary; all live-content and production gates remain closed. |
| D-083 | Post-publication operations use a typed secret-safe service boundary rather than exposing storage rows or coordinator internals. | Status reports contain only typed IDs, states, counts, dependency state, hashes, and generic failures; accepted prose, exact evidence, request/result JSON, prompts, provider details, and protected identity stay private. |
| D-084 | Raw retry/recovery rationale is not durable CERA data. | Attempts, recovery errors, and operator receipts retain only `authorization_reason_sha256`/`reason_sha256`. Exact work identity plus the caller's current rationale still gates retry. |
| D-085 | Operator mutations remain explicit commands and cannot broaden story authority. | Pending dispatch, interrupted recovery, and exact failed-work retry return schema-registered receipts with `story_state_retained: true`; they cannot undo accepted prose or silently retry/fallback. |
| D-086 | ChatGPT Pro returned advisory verdict `PHASE_19_OPERATOR_SERVICE_ACCEPTED`. | No in-scope correction was required. The verdict accepts only the provider-free secret-safe operator boundary; every live-content, product-transport, and production gate remains closed. |
| D-087 | The broad provider-free ordinary path has one production-prohibited application facade. | It accepts typed prepared ordinary input, runs the existing validated pipeline/publication/work components, and returns accepted presentation-neutral prose plus safe receipts/report; it is not a SillyTavern or production transport. |
| D-088 | Exact application replay is resolved before all adapters and downstream dispatch. | Request/branch/idempotency/source identity plus immutable work-plan compatibility returns the stored artifact with zero calls. Pending/failed work remains under explicit operator commands and cannot be retried by message replay. |
| D-089 | Pre-commit failure and post-commit downstream failure are different application outcomes. | Pre-commit failures return a stable no-commit error. Renderer/consolidator failure retains and returns the accepted artifact plus a sanitized operational report; it cannot turn a committed story into a failed story turn. |
| D-090 | ChatGPT Pro returned advisory verdict `PHASE_20_PROVIDER_FREE_APPLICATION_ACCEPTED` and confirmed the broad provider-free ordinary path is complete. | No further provider-free ordinary milestone is required. Work stops at the creator gate; live story roles, Adult EX/adult routing, production binding, SillyTavern transport/integration, and production operations each require explicit creator selection and authorization. |
| D-091 | The creator-requested post-phase acceptance test is a 20-run provider-free application matrix, not implicit authorization for a closed gate. | It must cover all seven characters, multi-character turns, indirect memory, fork/regeneration, downstream completion, exact replay, database integrity, and full regression. The completed run produced 20 artifacts/events, 60 terminal work items, zero failures/replay calls, and 256 passing repository tests. |
| D-092 | ChatGPT Pro returned `FINAL_20_RUN_ACCEPTANCE_ACCEPTED`. | Phases 1-20 and the creator-requested provider-free 20-run test complete the active authorized goal. The verdict grants no live, adult, product, production, promotion, or deployment authority. |
| D-093 | The creator separately authorized the provider-free Adult ON/EX catalog and routing integration gate. | CERA may preserve/hash the 24 audited Sera adult sources, compile approved material into CERA-owned fragments, implement typed plan-derived selection/specificity/repair contracts, and test with fake adapters and disposable databases. Live providers, adult publication, production data/world binding, SillyTavern, external handlers, promotion, and deployment remain closed. |
| D-094 | Preserved adult source artifacts are immutable provenance; compiled section-level fragments are the only runtime craft assets. | Sera paths never become runtime dependencies. Character/Genesis authority, obsolete Director/Detailer ownership, and coverage-claim files are excluded from runtime craft. Catalog claims cannot prove candidate specificity. |
| D-095 | Runtime Codex specifies adult craft semantically and Python selects the actual fragments. | `AdultCraftNeed` is allowed only on a consent-valid adult `ReasonerOutcome`; it carries families, axes, beat/channel needs, two-axis climax/aftermath state, and selected character-card sections, but no craft prose. Python owns coverage/budget selection and fails closed when incomplete. |
| D-096 | Adult specificity is beat- and channel-scoped, not a global vocabulary quota. | Narration, dialogue per character, inner voice per character, sound effects, and physiology remain distinct. Direct/vulgar requirements require an actual compiled lexicon. Anal and toilet/scat remain separate families. Conditional layers are not automatic requirements. |
| D-097 | The provider-free adult path permits at most one typed beat-scoped repair followed by complete validation. | Repair is allowed only for one failed beat, locks every non-target span, preserves authority/route/decision/branch, and must pass specificity plus the entire Composer structural validator. It cannot retry recursively or commit. Live repair remains unpromoted. |
| D-098 | Adult craft integration advances the contracts whose canonical serialization changed. | `ReasonerOutcome` is v2, `SceneComposerRequest` is v3, and `LiveShapedTurnReceipt` is v2. Earlier versions keep their historical meanings; the new versions prevent old hashes from silently acquiring new semantics. |
| D-099 | ChatGPT Pro's first implementation review returned `CERA_ADULT_ON_EX_PROVIDER_FREE_CORRECTIONS_REQUIRED`. | Lexical beat/channel coverage plus structural validation did not establish central-act clarity, actor/action/object association, transition order, material-result consistency, or false coverage claims. This was a material in-scope validator gap, not a request to weaken authority or open a live gate. |
| D-100 | Adult acceptance requires one provider-neutral semantic-specificity verification per candidate stage. | The request contains only locked beat-local prose, the specificity contract, safe selected IDs, action/object/material bindings, and required transitions. Its typed result/receipt cannot add authority, trust Composer declarations, retain prose, retry, fall back, or commit. A scripted fake supplies provider-free evidence only. |
| D-101 | Post-repair semantic failure ends the turn. | One beat may be repaired only after initial deterministic and semantic checks. The spliced candidate receives one fresh semantic request and then full Composer validation. Any semantic failure at that stage is non-repairable; no second repair, fallback, or story write is allowed. `BeatScopedRepairRequest` advances to v2 and `LiveShapedTurnReceipt` to v3. |
| D-102 | ChatGPT Pro returned the exact final verdict `CERA_ADULT_ON_EX_PROVIDER_FREE_ACCEPTED`. | The corrected provider-free Adult ON/EX catalog and routing gate is accepted. This closes the required advisory review and grants no live provider call, adult publication, production data/world binding, SillyTavern or external-handler integration, route promotion, or deployment authority. |
| D-103 | The creator authorized exactly five bounded live story-role qualification attempts and installation of a SillyTavern character/prompt shell named `Hanezawa Family - Cera v1.0`. | The cases may use disposable real-Genesis databases, Sol-medium, and DeepSeek V4 Pro with no retry/fallback/publication. The card may be imported locally, but no production world, promotion, deployment, or legacy Vera fallback is authorized. |
| D-104 | The first five-run live qualification is terminal evidence: 0/5 cases passed and no case may be silently rerun. | Failures were one missing required indirect-memory citation, one overstrict harness responder assertion after the validated pipeline returned, one AdultCraftNeed/current-beat mismatch, and two failed Codex MCP lookups. All cases retained zero story writes and no canonical state. |
| D-105 | Adult craft beat needs target the non-empty subset of current beats that actually require adult-specific craft. | Requiring every current beat duplicated non-craft pauses/reactions and caused an avoidable live contract failure. Beat IDs must remain current and exact; future or invented beats remain invalid. |
| D-106 | Codex evidence tools are conditionally exposed, superseding D-063's unconditional bridge requirement. | When Python has assembled a sufficient exact seed dossier, `CodexSceneReasonerPort` may run with no MCP binding and retains the provider receipt without fabricating a bridge receipt. When investigation is required, the request-bound bridge remains mandatory and fail-closed. |
| D-107 | The installed SillyTavern card is a presentation shell, not a ready runtime. | It routes only to the CERA development identity `cera-alpha` at `127.0.0.1:5101`, clears foreign host memory, carries no World Info, and never falls back to Vera port 5100. Chat readiness remains false until a separately authorized CERA-native adapter exists. |
| D-108 | ChatGPT Pro returned the exact advisory verdict `CERA_LIVE_FIVE_CORRECTIONS_ACCEPTED`. | No concrete in-scope defect remains in the provider-free correction package. The verdict does not alter the terminal 0/5 result, convert any case into a pass, establish live Reasoner/Composer/adult/prose quality, authorize a rerun, or make the SillyTavern shell chat-ready. |
| D-109 | The creator authorized one fresh corrected five-case live qualification batch using Sol-medium and DeepSeek V4 Pro. | The batch must have a new qualification identity and evidence directory, attempt each case once, preserve v1, make no story commit, and perform no retry, fallback, product activation, production binding, promotion, or deployment. ChatGPT Pro review follows the terminal result. |
| D-110 | The D-109 `five-run-live-v2` batch is terminal evidence at 0/5 passed. | V2 preserved v1 and made zero retries/fallbacks/writes. Prior retrieval, MCP, beat-subset, and harness failures advanced, but live outputs exposed one unsupported Composer semantic category, one invalid participant realization, and one repeated AdultCraftNeed channel-owner mismatch. No accepted prose or qualified route exists. |
| D-111 | ChatGPT Pro returned the exact advisory verdict `CERA_LIVE_FIVE_V2_DIAGNOSIS_ACCEPTED`. | Keep the existing validators; correct provider-side semantic enum, participant-realization, and channel-owner guidance/schema provider-free, and retain privacy-safe provider/context receipt evidence on failed typed decoding. The verdict grants no implementation, rerun, promotion, product, production, publication, or deployment authority. |
| D-112 | The creator authorized the provider-free Structural Contract v2 correction phase on 2026-07-29. | CERA may add raw ingress, provider-neutral intent interpretation with fake adapters, Python seed assembly/reauthorization, corrected retrieval/index contracts, advisory Reasoner/Composer DTOs, Python-derived bookkeeping, beat-local AdultCraftNeed v2, independent realization verification, privacy-safe failure/stage journals, source-inventory checks, migrations, docs, and broad offline tests. Every live, product, production, publication, promotion, retry, fallback, handler, and deployment gate remains closed. |
| D-113 | Provider-owned bookkeeping does not protect story quality and is removed from active provider DTOs. | Codex provides semantic decisions/local keys/evidence IDs; Python derives canonical IDs, hashes, citations, versions, labels, route flags, and `ReasonerOutcome`. DeepSeek provides prose plus anchored coverage/realization evidence; Python derives manifests. `SemanticInference` and provider-owned realization booleans are historical compatibility records, not active provider obligations. |
| D-114 | Objective events and durable derived memory require independently accepted realization evidence. | `SceneRealizationVerifierPort` is provider-neutral; only scripted fake adapters exist in this phase. Rejected, inconclusive, missing, malformed, or incomplete verification ends before publication. A Composer assertion or anchor alone cannot establish that a beat or participant was realized. |
| D-115 | Pre-publication failure evidence is privacy-safe, append-only, and non-resumptive. | `TurnFailureEvidenceBundle` and `TurnStageAuditEntry` preserve hashes, counters, typed receipt handles, and stage order without raw source, prose, private evidence, prompts, or secrets. Valid provider/context/tool receipts survive later failures. Restart inspection never auto-repeats a provider call. SQLite migration 11 owns these journals. |
| D-116 | ChatGPT Pro returned the exact advisory verdict `CERA_STRUCTURAL_CONTRACT_V2_ACCEPTED`. | Codex independently found the verdict consistent with the final provider-free implementation and 285-test run. This closes only the Structural Contract v2 review; it grants no live qualification, product, production, publication, promotion, handler, retry, fallback, or deployment authority. |
| D-117 | The creator authorized one fresh five-case Structural Contract v2 live qualification on 2026-07-29. | Use new immutable evidence identities; preserve v1/v2; make no retry, fallback, story commit, production binding, promotion, or deployment. |
| D-118 | The D-117 `five-run-live-v3-structural-contract-v2` batch is terminal evidence at 0/5. | Every case was rejected before model execution because the live Codex structured-output dialect forbids the nested `oneOf` in the AdultCraftNeed v2 provider schema. No Codex output or DeepSeek call occurred. This exposes a provider-schema projection gap, not five story-quality failures, and grants no correction or rerun authority. |
| D-119 | The creator authorized the provider-schema compatibility correction, advisory Pro review, one non-story Sol-medium schema probe, and—only after probe success—one fresh five-case live batch. | Preserve authoritative Python contracts and v1-v3 evidence; implement versioned provider projections and broad offline tests. The live probe and any later cases are one-shot with no retry/fallback/write/promotion/product/production/publication/handler/deployment authority. |
| D-120 | Provider schemas are versioned transport projections, not domain authority. | OpenAI strict-schema projection uses `openai_structured_output_2026_07_v1`; DeepSeek prompt-schema projection uses `deepseek_json_object_prompt_2026_07_v1`. Every boundary is inventoried and recursively preflighted, and all returned objects still pass unchanged authoritative Python decoding and cross-field validation. |
| D-121 | ChatGPT Pro returned `CERA_PROVIDER_SCHEMA_COMPATIBILITY_ACCEPTED` with no required correction before the probe. | Pro disclosed that it could not inspect the D-drive files line by line; Codex independently checked the advisory reasoning against the implementation, 291-test result, and provider contracts and accepted it within that limitation. This closes only the advisory gate and does not itself qualify a live route. |
| D-122 | The D-119 non-story Sol-medium full-Reasoner-schema probe passed. | OpenAI accepted provider-schema hash `9c2545e6…1aede`; authoritative Python decoded the result. The immutable receipt records one call, zero retry/write, request/output hashes, model/effort, latency/usage, and no retained prompt/raw output. |
| D-123 | The conditional D-119 v4 five-case batch is terminal at 1/5 structurally passed. | V4 corrected v3's handshake and advanced all cases to model output. Failures were one incomplete decision-ready Reasoner draft, one invalid DeepSeek quote occurrence, and two duplicate AdultCraftNeed scene-channel concept sets. The one structural pass still exposes an unsupplied protected-user narration concern under the explicitly non-proving fake verifier. No retry, fallback, story write, promotion, publication, or product/production action occurred. |
| D-124 | The creator authorized the provider-free V4 diagnostic and structural correction phase on 2026-07-29. | Treat v4 as probes, correct shared status/diagnostic/uniqueness/anchor/evidence-export/protected-user verification classes with fakes and disposable databases only, preserve all live evidence, obtain Pro advisory review, and stop before later live authority. |
| D-125 | Quote occurrence and semantic acceptance are no longer self-attested provider bookkeeping. | DeepSeek supplies a unique exact quote plus a zero compatibility sentinel while Python derives offsets; realization acceptance requires an independent protected-user semantic-boundary check, and protected-user rejection requires a Python-validated anchored finding. Scripted/echo fakes are not live-qualification evidence. |
| D-126 | Failure exports must preserve every privacy-safe receipt handle crossed before the failure. | Failure bundles and future qualification records retain the complete safe Reasoner/context/Composer/verifier receipt chain and lookup handles, while raw source, prose, prompts, private evidence, and secrets remain excluded. |
| D-127 | ChatGPT Pro returned `CERA_V4_PROVIDER_FREE_STRUCTURAL_CORRECTION_ACCEPTED` with no required correction. | Codex independently accepted the advisory result against the final implementation and 298-test evidence. The semantic-verifier condition applies at future qualification dispatch, receipt completeness proves traceability rather than semantic correctness, and the verdict grants no live/provider/product/production authority. |
| D-128 | The creator authorized implementation and one-shot qualification of a Sol-medium `SceneRealizationVerifierPort` on 2026-07-29. | Implement provider-free tests first, then make at most one independent non-story Sol-medium semantic-verification call after composition. The verifier may inspect and classify realization but may not generate, continue, revise, repair, or replace story prose. Python remains final validation authority; retry, fallback, five-case rerun, story commit, product/production activation, publication, promotion, handler attachment, and deployment remain prohibited. If the probe fails, stop and report the exact safe failure. |
| D-129 | The D-128 provider-free gate passed 304/304 and the one-shot Sol-medium non-story verifier probe passed. | One dispatch detected the expected unauthorized protected-user physical realization, Python validated and anchored it, and safe receipts record zero retry, fallback, or authority writes. This minimally qualifies the adapter's schema/transport/semantic-rejection path, not arbitrary semantic coverage, a full story route, human quality, production, publication, or promotion. No second probe or five-case rerun is authorized. |
| D-130 | ChatGPT Pro returned `CERA_SOL_REALIZATION_VERIFIER_QUALIFICATION_ACCEPTED` with no required correction. | Codex independently accepted the advisory verdict against the 304-test gate and one-shot evidence. The result is only a structural adapter qualification and one negative semantic canary. `qualification_eligible` permits participation only in a later separately authorized qualification dispatch; it does not establish general semantic accuracy, a qualified story route, production, publication, or promotion. |
| D-131 | After D-128/D-130 completed, the creator authorized Codex to continue with the next named gate and then process the supplied Hanezawa V1.2 package. | Run one fresh five-case full-pipeline live qualification with new immutable evidence identities, Sol-medium Reasoner, DeepSeek V4 Pro Composer, and the Sol-medium realization verifier. Each case/stage remains one-shot with no retry, fallback, story commit, production binding, publication, promotion, SillyTavern activation, handler attachment, or deployment. Close and review that batch before inspecting or changing the Hanezawa package. |
| D-132 | The D-131 `five-run-live-v5-sol-verifier` batch is terminal at 1/5 structurally passed. | The ordinary indirect-memory path passed all three live stages. One ordinary candidate was rejected for unsupplied protected-user realization, one Adult ON output failed exact quote anchoring, and two Adult EX cases exposed semantic-concept versus lexicon-requirement conflation. Craft-selection failures also discarded crossed Reasoner receipt evidence, while verifier failure retained only receipt handles after its sandbox disappeared. No retry, fallback, story write, rerun, promotion, or production action is authorized. |
| D-133 | ChatGPT Pro returned `CERA_LIVE_FIVE_V5_DIAGNOSIS_ACCEPTED`. | Codex independently accepted the advisory finding: case 2 is failed but indeterminate; semantic versus lexical AdultCraftNeed ownership, Python-concatenated Composer segments, universal safe failure-receipt export, and separately governed rejected-candidate review evidence form the minimum later provider-free correction package. V5 remains terminal and the review grants no patch, rerun, provider, product, production, publication, promotion, handler, SillyTavern, or deployment authority. |
| D-134 | The creator authorized installation and provider-free integration of the supplied Hanezawa V1.2 complete package after the D-131 live gate closed. | Preserve the archive and all supplied artifacts with verified hashes; keep V1.1 immutable; compile V1.2 as an explicit child revision; generate derived views and retrieval indexes; integrate bounded active-speaker expression evidence; use only disposable databases and no provider, story, production, product, publication, promotion, deployment, push, or live-state action. |
| D-135 | Hanezawa V1.2 expression quality is evidence-driven through existing Reasoner semantics and Composer context, not a new model-owned response manifest. | Codex selects current meaning and exact evidence through existing move/beat citations. Python owns scope, exact-section expansion, privacy, participant applicability, deterministic bookkeeping, and validation. DeepSeek receives only relevant cited active-speaker expression records and creates fresh wording. Examples are non-executable and noncopyable. |
| D-136 | V1.2 supersedes V1.1 only inside the new child revision; V1.1 remains immutable prior-version evidence and the default production-prohibited evaluation seam remains unpromoted. | Disposable V1.2 installation first installs the parent and then the child, retires every active parent record through explicit supersession, preserves historical records, and leaves existing V1.1 defaults unchanged unless a caller explicitly selects `revision="v1_2"`. |
| D-137 | ChatGPT Pro returned `CERA_HANEZAWA_V1_2_PROVIDER_FREE_ACCEPTED` with no required correction. | Codex independently accepted the advisory verdict against the repository implementation and 331-test evidence. Reusing existing Reasoner citations and bounded Composer context is preferable to a second speech manifest. Optional wording and broader blind-review suggestions remain non-blocking and do not authorize mutation of the hash-bound creator prompt, provider calls, live qualification, production/default Genesis promotion, publication, activation, or deployment. |
| D-138 | After Codex identified the provider-free v5 correction as the immediate gate before another live qualification or SillyTavern human test, the creator instructed Codex to continue. | Implement only the shared provider-free v5 correction classes with fake adapters/disposable data: semantic-versus-lexical AdultCraft ownership, Python-anchored Composer segments, universal privacy-safe failure receipt export, separately governed rejected-candidate review evidence, schemas, prompts, tests, and documentation. No live call/rerun, persistent story world, SillyTavern activation, production binding, publication, promotion, handler, or deployment is implied. |
| D-139 | Structural Contract v3 is additive and preserves immutable qualification history. | Active Reasoner and Composer provider DTOs advance to v3; Python compiles `AdultCraftNeedV3` and authoritative `ReasonerOutcome` v4. Historical v2 DTOs, `AdultCraftNeedV2`, and `ReasonerOutcome` v3 remain registered decoders. `TurnFailureEvidenceBundleV2` may retain only allow-listed canonical safe receipt payloads; rejected prose is retained only by an explicitly installed creator-local qualification review port and never enters runtime receipts or story authority. |
| D-140 | ChatGPT Pro returned `CERA_STRUCTURAL_CONTRACT_V3_PROVIDER_FREE_ACCEPTED` with no required correction. | Codex independently checked the advisory conditions against the actual source and 339-test result: semantic/lexical craft ownership is separate, active Composer v3 has one Python-concatenated prose authority, safe receipt export is allow-listed and fail-closed, rejected-candidate review is opt-in/noncanonical, and no retry or fallback was added. This closes only D-138's provider-free correction and grants no live call, rerun, persistent world, product activation, publication, promotion, handler, or deployment authority. |
| D-141 | The creator authorized one fresh Structural Contract v3 live gate on 2026-07-29. | First run one minimal non-story Sol-medium Reasoner-v3 schema-acceptance probe. Only if it passes, run one new five-case batch with Sol-medium Reasoner, DeepSeek V4 Pro Composer, and Sol-medium Scene Realization Verifier. Every case and provider stage is one-shot; v1-v5 evidence remains immutable; retry, fallback, story commit, persistent world, product activation, publication, promotion, handler, and deployment remain prohibited. |
| D-142 | The D-141 qualification runner explicitly installs the D-139 creator-local rejected-candidate review port. | A verifier rejection writes one hash-named, overwrite-refusing artifact under the new qualification directory. It remains qualification-only, noncanonical, runtime-receipt-forbidden, manually retained for creator adjudication, and unable to write story authority. The provider-free suite remained 339/339 before either live gate was consumed. |
| D-143 | The one-shot D-141 Sol-medium Reasoner-v3 schema probe passed. | OpenAI accepted projected schema hash `3789d98b...19ee8e`; authoritative Python accepted the returned v3 draft. The immutable record contains one provider call, zero retry, zero story-authority writes, and no prompt or raw output. This consumed the probe and opened only D-141's conditional five-case batch. |
| D-144 | The D-141 `five-run-live-v6-structural-contract-v3` batch is terminal at 0/5. | One Reasoner MCP search failed its input contract after prior bounded lookups; one ordinary Composer candidate was correctly rejected for invented protected-user behavior; one adult Composer used the adult channel label `physiology` as an invalid general realization kind; one structurally accepted climax candidate lacked a recognized sound-effect span and had no authorized repair port; and one toilet candidate duplicated source-unit coverage references. There were zero retries, fallbacks, story commits, or story-authority writes. V1-v5 remains unchanged and no rerun or correction is authorized. |
| D-145 | ChatGPT Pro returned advisory verdict `CERA_LIVE_FIVE_V6_DIAGNOSIS_ACCEPTED`, and Codex independently accepted the bounded review. | All five diagnoses remain controlling. Case 1 proves an evidence-bridge contract and safe-diagnostic defect; stop-after-obligation-satisfaction is a preventive guard, not a proven exact trigger. Case 2 is a determinate protected-user true positive. A later separately authorized provider-free package should move realization/coverage identity to Python-owned obligations and ordered unique segment-key mappings while preserving strict validators. The review authorizes no correction or rerun. |
| D-146 | The creator's 2026-07-29 overnight goal authorization supersedes D-145's no-correction/no-rerun gate for the bounded development objective. | Codex may independently audit, restructure, implement, test, make bounded live Sol/DeepSeek calls, build a local SillyTavern development adapter, run a fresh continuous ten-turn qualification with no retry/fallback, and reset disposable story state. Immutable Genesis/Adult provenance and all creator, identity, privacy, knowledge, branch, supersession, consent/capacity, participant, protected-user, evidence, and atomic-publication boundaries remain hard. Production binding, deployment, route promotion, an external handler, and any silent fallback remain excluded. ChatGPT Pro is advisory; Codex owns final technical judgment. |
| D-147 | D-146's remaining 14 Sol calls permitted the minimum four-call non-story persistent transport qualification without raising the 50/25 ceilings. | Two sequential Sol-medium Reasoner calls passed through one fixed no-MCP process and two sequential Sol-medium Verifier calls passed through a separate process. Every request used a fresh ephemeral thread/workspace, produced a unique safe receipt, used zero tools/retry/fallback/story writes, and closed cleanly. Exact summary SHA-256 `f92a08e5cda04c701c74b3d1ddd98fe1abc0da733959f56545c0eb43cd07f5f3` is the activation authority; process reuse is qualified, not story semantics. |
| D-148 | Persistent transport integration is mixed rather than universal, and the next route must be materially different from v11. | No-MCP Reasoner turns and all Verifier turns use separate qualified persistent runners; a request-bound MCP Reasoner turn remains isolated one-shot. V12 follows a new Tomi/Yuuni/Aoi path with paraphrased `T-M07` retrieval, selected floor/context, restart, fork, regeneration, and relationship continuity. The complete provider-free suite passes 376/376. The live ledger is 40/50 Sol and 18/25 DeepSeek, so v12 remains locally blocked until total ceilings reach at least 60/28. |
| D-149 | ChatGPT Pro returned advisory verdict `CERA_PERSISTENT_TRANSPORT_AND_V12_ACCEPTED`, and Codex independently accepted its narrow findings. | Transport selection remains Python-owned, capability-driven, and fixed before dispatch; no failed request may cross into the other route. Exact evidence bytes plus semantic revalidation gate activation. `T-M07` remains fixture content, not a transport special case. No further provider-free correction is required before the creator call-ceiling decision, but the advisory verdict authorizes no call, story write, product activation, or deployment and proves no ten-turn route. |
| D-150 | The creator's 2026-07-29 instruction grants total live-call ceilings of 500 Sol-medium and 500 DeepSeek V4 Pro calls for the active D-146 goal. | This supersedes only D-146's 50/25 spending ceiling. It does not weaken one-attempt/no-retry/no-fallback semantics, immutable evidence identities, provider/model assignments, validation, privacy, branch, consent, protected-user, publication, production, handler, promotion, or deployment boundaries. The conservative ledger starts this authority at 40 Sol and 18 DeepSeek already consumed. |
| D-151 | Continuous live qualification v20 passed 10/10 consecutive turns under D-146/D-150. | The disposable route used 20 Sol and 10 DeepSeek calls, one attempt per stage, no retry/fallback, and exercised paraphrased retrieval, selected multi-character context, restart, fork, regeneration, physical/emotional response, and relationship continuity. Integrity and foreign keys were clean; the synthetic database was discarded. This qualifies one local ordinary development route, not production, public deployment, Adult ON/EX, or universal prose quality. |
| D-152 | The CERA-native SillyTavern v3 two-turn restart-continuity smoke passed through the actual local UI. | The disposable world committed two accepted artifacts, two direct events, and two system-private accepted-reply material records. After an adapter restart, Sakura repeated all three exact placement details from the first accepted reply. Six one-shot provider receipts show four Sol and two DeepSeek calls, zero automatic retry, and zero provider story-authority writes. The ordinary local human-test adapter is chat-ready after cleanup; Adult ON/EX live publication, production binding, promotion, deployment, and external-handler integration remain closed. |
| D-153 | ChatGPT Pro returned `CERA_LOCAL_HUMAN_TEST_GATE_ACCEPTED`, and Codex independently accepted the local ordinary-route gate. | Current-head artifact lineage, snapshot authorization, system-private visibility, and lower-authority MATERIAL semantics prevent accepted presentation prose from becoming event/Genesis truth or leaking from replaced and sibling artifacts. The active qualified prompts remain frozen; explicit inert-evidence wording is deferred to a separately versioned and qualified prompt hardening change. Creator manual ordinary-route testing may begin, while Adult ON/EX live publication, production, promotion, deployment, and handler gates remain closed. |
| D-154 | The creator's first manual SillyTavern turn exposed shared transport-metadata and Composer-bookkeeping compatibility gaps, which D-146/D-150 authorize Codex to correct and verify. | SillyTavern now sends typed CERA session/depth/regeneration controls; the adapter prefers typed fields, keeps legacy markers only for compatibility, and rejects conflicts. Python derives an exact protected-user realization kind only when source authority permits one unique value; all ambiguous or unauthorized cases still fail closed. A fresh disposable three-call probe and a one-shot replay of the original UI turn passed, the persistent human-test world advanced atomically to generation 1, and the complete provider-free suite passed 442/442. No retry, fallback, Adult ON/EX publication, production binding, promotion, handler, or deployment authority was added. |
| D-155 | The creator's second manual API error proved that SillyTavern New Chat identities were incorrectly sharing `branch:main`. | Each new CERA chat now resolves to an isolated deterministic root branch. Exact accepted-head transcript reconciliation preserves an existing chat across a client session-key change without allowing zero-match or ambiguous-match branch merges. Current CERA controls are typed transport metadata rather than hidden prompt instructions. Non-decision Reasoner outcomes terminate at a separately audited decision gate with privacy-safe prior-stage receipts. The exact failed second chat then passed one-shot on its own branch, both creator chats remained independently generation 1, and the complete provider-free suite passed 444/444. No retry, fallback, validator weakening, Adult ON/EX publication, production binding, promotion, handler, or deployment authority was added. |
| D-156 | Local human testing proved that response depth must be adaptive causal scope and that realization-character ownership is redundant provider bookkeeping. | Reasoner prompt v12 classifies atomic, developed ordinary, and domino/multi-scene replies without a minimum-beat validator. Composer prompt v17 realizes the complete supported sequence without padding, invented history, or extending past protected-user setup. Active Composer DTO v6 omits provider-authored `owner_id`; Python derives it from the validated beat/source authority and removes only exact duplicate bookkeeping before unchanged semantic validation. One atomic and one four-beat/two-character live probe passed; three intermediate failures remain immutable with zero story writes. This correction changes no Adult ON/EX, production, promotion, deployment, retry, fallback, or external-handler boundary. |
| D-157 | Creator manual testing showed that D-156 left depth advisory, omitted authoritative story-start context, obscured selected speech-system evidence inside a double-encoded Composer field, and lost the typed regeneration key during SillyTavern chat-change handling. | Scene depth is now a typed ingress-to-Reasoner contract: Long requires the full supported current-scene chain and Epic requires maximum supported NPC-controlled progression without numeric quotas or padding. Empty generation-zero branches receive the exact creator scenario projection with owner-bound privacy instructions. DeepSeek receives decoded selected-character evidence and explicit voice-example semantics. The installed extension preserves one in-flight regeneration key through request assembly, and regeneration seed continuity excludes the candidate head being replaced. Reasoner prompt v13/packet v8, Composer prompt v18/packet v12, and SillyTavern profile v4 pass 449/449 provider-free tests. No retry, fallback, validator weakening, adult publication, production binding, promotion, handler, or deployment authority is added. |
| D-158 | The creator directed CERA to use DeepSeek V4 Flash, not Pro, as the implementation and local SillyTavern Composer default for now. | `deepseek-v4-flash` is the single Python-owned default. Pro remains an explicit supported comparator and all historical Pro qualification evidence stays immutable; this decision does not reinterpret those results or qualify Flash prose. No silent fallback is allowed. A later model change requires an explicit configuration/decision and appropriate qualification. |
| D-159 | The creator authorized an inactive modular Composer compiler scaffold and frozen ChatGPT Pro authoring snapshot while requiring the current route to remain unchanged. | CERA adds provider-neutral typed registry, selector, Adult OFF/ON/EX rendering transport, deterministic compiler, fixture-only modules, receipts, and provider-free tests. Python owns exact module selection/loading/order/hashes; the Reasoner may supply only bounded semantic selector values. The seam has no active state, provider dispatch, publication, retry, fallback, or Detailer capability. Current Composer prompt v18 and `deepseek-v4-flash` remain the implementation defaults. Final writing-facing module prose, activation, provider qualification, Adult publication, production, promotion, handler, and deployment require later explicit gates. |
| D-160 | The creator authorized the complete behavioral-consolidation goal, including shared restructuring, live Sol/DeepSeek qualification, local SillyTavern changes, disposable-state reset, and a fresh ten-case gate. | Claim-level authority, `CharacterAutonomyMode` (default `both`), Adjustment/Modification, broad `SceneEventBlock` plus `WriterScaffold`, adaptive causal runway, bounded protected-user realization, effective-character projections, atomic development, provisional display, background Sol review, creator controls, and diagnostic records are controlling. CERA may change the roadmap to correct shared root causes. Immutable Genesis/Adult provenance and every hard creator, identity, privacy, knowledge, branch, supersession, consent/capacity, protected-user, evidence, and atomic-acceptance boundary remain hard. No silent retry or fallback is authorized. |
| D-166 | Creator manual testing exposed short Auto follow-through, incomplete accepted-context visibility, missing exact household-rule seeding, plausible Composer world-fact invention, and verifier evidence asymmetry. | Reasoner v23 retrieves and cites exact concrete world facts; household-information cues seed public rule records; Composer v28/prompt v26 uses closed-world protected-user and world-fact authority; verifier v8/request v7 receives the same bounded authoritative evidence selected for composition. Five real SillyTavern turns were accepted, Epic regeneration reached review without API error, and 501/501 provider-free tests pass. This is an ordinary/relationship human-test correction, not Adult publication, production, promotion, deployment, fallback, retry, or external-handler authority. |
| D-167 | The creator authorized a live Codex Reasoner model/effort ladder from Luna-high through Sol-medium, with two identical samples per route and reviewable timing, token, cache, logic, story, and API-error evidence. | CERA may make the bounded one-shot calls needed for this comparison under the existing 500/500 ceilings. The benchmark uses current real V1.2 context, DeepSeek V4 Flash thinking, and the fixed Sol-medium verifier in a disposable database. It makes no retry, fallback, story commit, production binding, or automatic route promotion. |
| D-168 | The D-167 terminal v3 ladder produced 16/16 structurally valid Reasoner decisions but only 4/16 accepted complete pipelines. | Eight DeepSeek calls did not yield one complete candidate, one failed an atomic Composer obligation, and three candidates failed protected-user semantic verification. No network/API transport error occurred. Terra-low was fastest; Sol-low is the best conservative shortlist candidate; Terra-medium's 2/2 mechanical passes contain floor-owner and scene-continuity defects. The current Sol-medium route remains unchanged pending shared Composer observability/reliability, floor-owner/continuity verification, Auto-scope correction, and a later diverse comparison. |
| D-169 | The creator authorized review and correction of the D-168 follow-up failure. | DeepSeek Flash composition defaults to explicitly disabled thinking because Codex already owns causal planning and the observed failure class was incomplete composition under a shared output ceiling. `ProviderFailureCallReceipt` v1 preserves exact normalized finish reason plus the existing safe timing/usage/model/hash receipt when a completed provider response is rejected, without retaining partial prose. Reasoner adapter/prompt v24 adds a complete non-ready reset instruction; Python validation remains unchanged. Any test-only continued Codex thread is invalidated after a failed pipeline stage. None of these changes permits invalid output, retry, fallback, story commit, route promotion, Adult publication, production binding, or deployment. |
| D-170 | The creator authorized three full CERA samples using GPT-5.6-Luna at its highest advertised reasoning setting and fastest advertised service tier. | The exact selection was Luna `max` plus `priority` (`Fast`). Terminal v3 finished 0/3 at the Reasoner stage: two supervised 240-second timeouts and one 16,384-token output-budget failure. DeepSeek and Sol verifier made zero calls; no retry, fallback, artifact, story write, production change, or promotion occurred. An earlier pre-dispatch serialization defect and one interrupted unbounded diagnostic dispatch are preserved separately. Sol-medium remains active. |
| D-171 | The creator selected Sol-medium as the current optimization target and directed an owner review of branch-bound persistent Codex sessions, treating ChatGPT's findings as advisory. | The measured `22k -> 13k -> 10k` sequence is uncached input, not shrinking logical context: total input grew `22,055 -> 34,610 -> 44,308`, while cached input grew. Process reuse is not conversation reuse. The recommended design preserves one accepted provider checkpoint per branch and forks every provisional candidate, correction, regeneration, and sibling branch under typed Python precedence. |
| D-172 | The creator authorized provider-free Branch-Bound Reasoner Session Architecture V1 and approved branch-local creator constraints by default with explicit global promotion. | CERA adds a shadow-only provider-neutral `ReasonerSessionPort`, durable session/checkpoint ledgers, typed authority deltas, acceptance/rejection receipts, exact creator constraints, regeneration/sibling isolation, compatibility invalidation, reconstruction/rotation, evidence materialization, prompt splitting, and privacy-safe usage observability. Raw rejected prose is not retained by V1. Restart-persistent non-ephemeral Codex rollouts, live calls, benchmarks, route activation, SillyTavern changes, publication, production, promotion, retry/fallback, and deployment remain closed. |
| D-173 | The creator authorized a five-run live test of the D-172 branch-bound Sol-medium path on 2026-07-31. | The terminal test passed 1/5: the cold v24 Reasoner turn passed, but app-server rejected all later `thread/fork` operations because the source thread was ephemeral and therefore had no persisted rollout. Only one Sol call occurred; DeepSeek, retries, fallbacks, story writes, route changes, and SillyTavern changes remained zero. This disproves provider-thread forking as an ephemeral-session implementation strategy and authorizes no silent switch to non-ephemeral retention. |
| D-174 | The creator authorized correction of the D-173 provider-session mismatch using Codex app-server's native stored-thread lifecycle. | D-172's accepted-checkpoint tree remains authoritative. Every provider checkpoint is now designed as a non-ephemeral stored rollout: a candidate is a stored leaf fork of its accepted checkpoint, acceptance promotes that exact leaf, rejection/failure deletes only that leaf, regeneration forks the replaced artifact's accepted parent checkpoint, and a CERA branch receives a distinct stored fork. Python remains final authority. SQLite migration 17 adds append-only privacy-safe custody events. The provider-specific adapter isolates the installed SDK's generated `thread/delete` RPC because its high-level facade does not expose deletion. This provider-free milestone does not activate the live route, call providers, alter SillyTavern, or authorize a rerun. |
| D-175 | The creator authorized CERA review/status access when SillyTavern is opened through the PC LAN address and requested an option for phone use. | CERA port 5101 remains loopback-only. SillyTavern now owns a same-origin, authenticated and CSRF-protected server relay exposing only health, exact review lookup, and typed creator decisions. Browser code no longer connects directly to port 5101. This supports local, LAN-address, and same-network phone UI access without exposing the CERA service or adding provider, retry, fallback, story, or authority behavior. |
| D-176 | The separately authorized native-stored Reasoner canary terminated before provider dispatch. | A ledger identity marked non-ephemeral is not sufficient to create a provider rollout. Resume and cleanup both reported no stored rollout; calls, retries, fallbacks, and story writes were zero, and the five-case batch did not start. The active route remains unchanged pending a provider-free stored-root lifecycle correction and a later separately authorized canary. |
| D-177 | The creator directed CERA to color review severities, port Sera-style character readability, add `False Positive`, and rename the visible correction action to `Adjustment`. | False Positive is available only for publication-eligible Concern/Critical assessments; it accepts the prepared candidate unchanged through the same atomic path and records a verifier-owned diagnostic that cannot become a story constraint. Exact validated visible dialogue may receive card-local presentation color, while accepted prose stays presentation-neutral. The action wire value `correction_adjustment` remains stable although its visible label is `Adjustment`. |
| D-178 | Native stored Codex threads require explicit materialization, and rejected/failed leaves are archived rather than deleted. | D-176's zero-call failure was caused by an empty non-ephemeral thread not yet having a durable rollout. CERA now names each newly allocated root/fork before process exit, proving it can be resumed from another app-server process. The installed app-server's delete path is not reliable in this environment, while archive is supported and makes the leaf non-resumable. Rejected and failed leaves are therefore archived, excluded from accepted ancestry, and retained only as advisory provider context plus privacy-safe custody hashes. Historical deletion event values remain decode-only compatibility records. SQLite migration 18 adds the archived terminal event values. This supersedes D-174's deletion mechanism without weakening branch isolation. |
| D-179 | The creator activated branch-bound native stored Sol reasoning for the local SillyTavern human-test route and requested selectable Medium, High, and Extra High effort. | A fresh non-story canary passed, then a five-turn Sol-medium stored-branch qualification passed 5/5 with one advancing checkpoint lineage, zero retry/fallback/DeepSeek/story writes, and successful leaf-first archival cleanup. The local route now reports `branch_bound_native_stored_v1` active. SillyTavern extension v1.3.0 exposes `Sol: M/H/Ex`, mapped to `medium/high/xhigh` for the Scene Reasoner only; the independent verifier remains Sol-medium. An effort change creates a compatibility change and reconstructs/rotates from Python's accepted branch state instead of mixing effort identities in one thread. |
| D-180 | A stored-thread human-test turn failed after Sol dispatch because one canonical evidence UUID was mistyped in one event block. | The validator correctly rejected and archived the candidate with zero story writes; this was neither network failure nor sibling-branch leakage. Adapter/prompt v25, packet v14, and MCP v7 replace provider-owned UUID copying with bounded request-local seed/fetch citation aliases. Python resolves only current allocated aliases to exact authorized evidence before unchanged semantic, privacy, branch, and publication validation. No retry, fallback, story replay, or automatic acceptance is introduced. |
| D-181 | The creator authorized a provider-free two-way review file bridge through Ted's Downloads folder. | Export validates and copies one immutable evidence ZIP; Wait watches only the exact response filename; Import requires matching checkpoint/evidence identity, preserves bytes and the first response, and records a privacy-safe receipt; Status never infers approval. ChatGPT Pro remains advisory, Ted authorizes every later tranche, and the bridge cannot call providers or touch story/database/runtime authority. |
| D-182 | The creator replaced the Downloads bridge as the primary workflow with a shared-repository overlapped Pro-Codex review cycle. | `pro_review_cycle.py` publishes identity-bound Jobs 1-3, starts only a hash-proven pre-authorized Job 4, then consumes the exact stable repository response after Job 4. Every non-bootstrap next package separately binds the preceding Job 4 result and current/revised Jobs 1-3. The supported Codex-app follow-up operation can activate an existing ChatGPT review chat and is recorded by hashed trigger receipt; filesystem publication alone is never called a trigger. V1 Downloads transport remains a manual emergency fallback. Pro responses remain advisory and cannot expand creator authority. |
| D-183 | The creator authorized correction of the 2026-07-31 SillyTavern `message failed` turn. | The failed fresh-chat turn stopped in the stored Scene Reasoner before a provider call, composer, verifier, artifact, or story-state write. CERA now durably projects value-free Reasoner transport/worker-stage details, returns those safe codes through the local SillyTavern error response, types stored-prompt boundary failures, and requires the repository `.venv` for the adapter so parent and child workers cannot use mismatched Python dependency environments. The original turn remains archived and uncommitted; no automatic retry, fallback, or replay is introduced. D-180 provider, prompt, schema, and active-route identities remain unchanged. |
| D-184 | The creator explicitly authorized one live test run of the pending doorway message through SillyTavern. | The same deterministic request resumed only through the creator-triggered UI action. Stored Codex reasoning, deterministic context assembly, DeepSeek composition, and Sol verification completed with exactly three provider calls and no retry or fallback. The candidate reached `review_ready` and rendered in SillyTavern, but Accept was not invoked; the active branch remains generation zero with no head artifact. This validates the repaired turn only and does not constitute production promotion, general reliability qualification, or creator prose acceptance. |
| D-185 | The creator authorized the Compact Reasoner V7 and input-preparation speed-optimization tranche with provider-free Progressions 1-3 and exactly four bounded Sol-medium calls in Job 4. | Progressions 1-3 add cumulative content-free Codex operation telemetry, explicit scene-cast scope, compact exact-evidence/capsule projections, and `CodexReasonerDraftV7Compact` plus a deterministic v7-to-v6 compiler. The D-180 v6 route remains active, selectable, and behaviorally unchanged; v7 is shadow/test only. Job 4 may compare the four authorized variants on a disposable database and isolated Git worktree, with no DeepSeek, verifier, story write, retry, fallback, Fast mode, route activation, deployment, or service change. |
| D-186 | The creator authorized Continuous Planner and Validator Session V1 as three provider-free progressions plus one separately bounded ten-call Job 4. | The shadow route uses one branch-bound continuous Planner thread, a physically separate continuous Validator thread, DeepSeek realization, candidate world directories, creator-gated atomic file promotion, and explicit Scene Change summaries. Python remains final authority. D-180 stays active; compact v7 remains defective and shadow-only. Progressions 1-3 cannot call providers, alter live story state, deploy, retry, or add fallback. |
| D-187 | The creator corrected the governed workflow after the first D-186 cycle: `corrections_required` is iterative, not automatically terminal. | Under an explicit standing creator authorization, Codex preserves each completed cycle, converts the exact Pro findings into the next one to three in-scope provider-free progressions, assigns new checkpoint/cycle identities, and returns to Stage 4. `blocked`, a new creator-policy decision, excluded authority, or a concrete tool/repository blocker remains terminal. Pro stays advisory and cannot authorize calls, production, story writes, deployment, service/installed-SillyTavern mutation, merge, remote, or push. |
| D-188 | The creator authorized correction checkpoint `2026-08-01-continuous-planner-validator-v1-corrections-001` and its provider-free Stage 4 audit. | Python allocates request-local exact evidence bindings, durably accounts provider dispatch before transport, restores D-177 False Positive semantics, stores Scene Summary only as a regenerable non-authoritative derived view, recovers directory promotion across process-loss cut points, restricts mutable created records to revisioned JSON objects, and records secret-safe root diagnostics. The failed prior Job 4 remains immutable and cannot be rerun under its old identity. Zero provider calls and no live/production effects are authorized. |
| D-189 | The consumed correction-cycle-001 review identified further in-scope provider-free authority, recovery, accounting, and inventory defects under D-187's standing iterative authorization. | Correction cycle 002 binds character summaries to exact ACTIVE fields or a package-bound Validator derivation, makes private evidence single-NPC actor-specific, classifies DERIVED evidence as retrieval-only, closes protected-user mechanical allowances, journals the complete acceptance transaction and pending model synchronization, counts only actual transport invocation, binds stored-thread hashes, wraps pre-provider setup, and reconciles persisted schema inventory. D-180 remains active and all provider/live/production effects remain excluded. |
| D-190 | The exact consumed correction-cycle-002 review identified short-canary harness drift, incomplete submission accounting, latent accepted-context authority, protected-user semantic gaps, incomplete successful synchronization custody, and unsafe candidate-derived character-summary provenance. | Correction cycle 003 aligns the harness with the generic runtime, marks invocation at the actual transport boundary, treats unresolved prepared calls conservatively, adds Python-owned exact protected-user source claims and owner-bound accepted-session evidence, atomically persists and journals the Planner snapshot before synchronization, forces derived character-summary MCP privacy, and disables candidate-derived character-summary authority. The work is provider-free; D-180 and every live/production exclusion remain unchanged. |
| D-191 | The exact consumed correction-cycle-003 review identified ambiguous protected-user source attribution, model-owned realization bookkeeping without exact occurrence proof, insufficiently scoped accepted-session context, mutable accepted-snapshot proof, imprecise Codex submission accounting, and a parallel Job 4 implementation. | Correction cycle 004 gives Python exact attribution-aware source claims and owner/scene-scoped accepted projections, requires DeepSeek exact claim realization spans, stores immutable typed snapshot receipts, counts Codex at `thread_run`, and routes the provider-free Job 4 harness through the shared coordinator. The work is provider-free; D-180 and every live/production exclusion remain unchanged. |
| D-192 | The exact consumed correction-cycle-004 review identified remaining heuristic ingress ownership, semantic protected-user invention after composition, incomplete authority-identity binding, participant-created future authority, whole-item privacy projection, stale compatibility identities, and short-canary qualification gaps. | Correction cycle 005 replaces text heuristics with explicit typed ingress units; requires exhaustive Composer actor/subject/speaker segments and exact protected-user claims; binds candidate, authority, prompt, claim, realization, segment, and accepted-projection hashes through storage and acceptance; projects accepted context as public and one-owner field facts; rejects stale continuous-session compatibility; parameterizes new canary identities; removes the repeated Turn 2 Sakura card; and tests the exact ten-stage fake harness plus real subprocess progress markers. The tranche is provider-free, D-180 remains active, and live canary authority remains closed. |
| D-193 | The exact consumed correction-cycle-005 review found that caller-supplied ingress units lacked trusted custody, subject-only or mislabeled roles could bypass protected-user authority, unprotected edits could diverge from final fields, event participants remained model bookkeeping, the audit test ID was stale, and the provider-free harness/process proof was incomplete. | Correction cycle 006 adds a Python-owned ingress receipt bound to every raw-turn identity; replaces actor/subject ambiguity with versioned assertion-owner and non-owning roles; propagates those roles into final fields, events, accepted facts, candidate/acceptance identity, and pre-v6 incompatibility; makes every edit and created field an exact cited-final-field projection; derives event roles and participants in Python; preflights declared unittest IDs; and runs the actual Planner/Composer/Validator ports through scripted transports plus a launch-to-timeout child-process matrix. The tranche remains provider-free, D-180 remains active, and every live/product/production exclusion remains closed. |
| D-194 | The exact consumed correction-cycle-006 review found that prepared ingress still trusted caller-supplied hashes, fixture prefixes were open authority, receipts were process-local, Composer roles could launder protected assertions, persistence destinations were unbound, and the scripted qualification bypassed the actual CLI entry point. | Correction cycle 007 connects ingress to exact durable prepared records, closes and hashes fixture registries, reconstructs receipts after restart, adds independent exact-span protected-semantic adjudication, binds add/replace persistence to exact typed targets and preconditions, advances every affected compatibility identity, and executes the actual Job 4 CLI through one hash-bound provider-free scripted mode. D-180 remains active; provider/live/product/production effects remain excluded. |
| D-195 | The exact consumed correction-cycle-007 review found that the durable prepared bridge was not called by a production-shaped shadow entry point, classifiers were trusted by adapter string, Character/Relationship writable fields were open, relationship subjects were not tied to cited final-field roles, and candidate records were not revalidated after edits. | Correction cycle 008 adds the repository-controlled SillyTavern-compatible shadow ingress path, a source-bound closed classifier registry, an explicit Character/Relationship semantic write allow-list, field-justified relationship ownership, complete post-edit record validation, V8 compatibility identities, adversarial semantic and persistence tests, a scripted-V8 CLI path crossing prepared and frozen ingress, and a separately creator-gated short-canary specification. D-180 remains active; provider/live/product/production effects remain excluded. |

## 2. Explicit supersessions

| Earlier draft assumption | Status | Replacement |
|---|---|---|
| Implementation target is `D:\AIChatBot\Vera_v2_d3`. | Superseded | D-001 |
| E-drive staged prompt files become runtime sources. | Superseded | Reference/provenance only; later authorized copy/adaptation into CERA |
| Core is named `CharacterDirectorPort`. | Superseded | Provider-neutral `SceneReasonerPort`; “Character Director” may remain a prose role label only |
| Combined DeepSeek one-call route is the live baseline to preserve. | Superseded for CERA | Greenfield pipeline; no inherited live baseline |
| Planner -> Writer -> automatic Detailer is the target. | Superseded | Reasoner -> Composer; conditional Adult Mechanics enrichment; optional enhancement only after qualification |
| A DeepSeek Adult Planner may independently expand psychology or receive exact source by default. | Superseded | D-030 and D-031: mechanics-only enrichment from the safe ledger; exact protected source remains Composer-restricted |
| Long/epic mode forces a Detailer. | Superseded | Complete Core remains default; length is not stage authority |
| A rejected Detailer falls back to Core. | Reframed | No default Detailer. If a future optional enhancement is rejected, the already accepted Core remains the artifact; no provider substitution occurs |
| Exact protected non-consensual source can go directly to DeepSeek for realization. | Superseded | Blocker/checkpoint; no Codex or DeepSeek generation through the event |
| A non-graphic causal ledger allows Codex to plan continuation of any event. | Narrowed | It may represent consent-valid protected mechanics or a later receipt; blocked event generation still stops |
| Semantic rewriting may make an act “accidental” or make discomfort resemble consent. | Rejected | D-018; abstraction cannot change intent or consent |
| External generator output can be returned as a ledger and continued. | Superseded | Only a bound, validated `ExternalCompletionReceipt`; no raw output and no automatic truth |
| The rejected user turn records that the described event happened. | Rejected | A `RejectedTurnReceipt` records an operational rejection only |
| Temporary projection becomes trauma memory. | Rejected | Projection is hypothetical scratch state; memory is created only from committed evidence |
| Hana follows the supplied tricked/manipulated trajectory. | Superseded | Trajectory is noncanonical; current evidence determines response |
| Husband's cheating remains objectively unresolved and Sakura/Enne merely suspect it. | Superseded | D-037; V1.1 establishes direct digital evidence known only to Enne and Sakura while Hana remains unaware |
| V1.1 leaves the husband's paternity of Mia and Yuuni unresolved. | Superseded | D-036; direct creator authority establishes him as biological father of both |
| V1.1 permits Chiyo to remain an unresolved active backstory entity. | Superseded | Chiyo is excluded from active Genesis; historical mentions remain provenance only |
| V1.1's Anima prefix, global negative, and per-character drift negatives compose the runtime prompt. | Superseded | D-038; the later creator overlay is the runtime prompt policy |
| Yuuni may never be rendered childlike in any context. | Narrowed | D-039 separates deliberately youthful ordinary visuals from age-unambiguous protected imagery |
| Provider failure may use a qualified fallback or blank retry. | Superseded | Explicit error and no state change; bounded repair is disabled until separately promoted |
| ChatGPT Pro/interactive Codex access is automatically usable by runtime. | Rejected assumption | A runtime adapter needs an explicitly qualified transport |
| Ted normally uploads/downloads and renames Pro review files or starts Wait/Import. | Superseded | D-182 repository-local cycle; D-181 remains emergency fallback only |

## 3. Clarifications that are not contradictions

- Codex may propose state candidates, but Python owns authority and commit.
- A composer may add reversible local texture, but consequential facts require authority or validated integration.
- A summary can be regenerated; the accepted prose and cited events remain immutable.
- Genesis is immutable at runtime, but a later explicit creator migration can version it. That is not character development.
- A reasoner may see labeled owner-private evidence required for its role; it may not transfer that knowledge between characters.

## 4. Change procedure

Every future decision change must include:

1. a new decision ID and creator evidence;
2. affected contract/schema versions;
3. migration or compatibility impact;
4. an explicit supersession link;
5. updated handoff and roadmap;
6. tests required before promotion.
