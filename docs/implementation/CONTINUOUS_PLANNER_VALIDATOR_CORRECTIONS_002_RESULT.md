# Continuous Planner/Validator Corrections 002 Result

**Decision:** D-189

**Scope:** provider-free shadow/test correction only

**Active route:** unchanged D-180

## Outcome

The three correction-cycle-002 progressions repair shared authority and
recovery classes identified by the exact consumed correction-cycle-001 Pro
review. They do not activate the D-186 route or qualify live provider behavior.

### 1. Summary and actor evidence authority v2

- Python builds and validates every character summary from exact stable fields
  in an ACTIVE character record, or from a Validator-derived record bound to
  an exact retained Validator package and exact ACTIVE source.
- Summary character, path, authority class, revision, source hash, JSON field
  pointers, text, latest accepted changes, and derivation receipt are checked
  before Planner dispatch.
- ACTIVE bindings are hard authority. DERIVED bindings are navigation/context
  and cannot alone support a hard character, rule, event, or memory decision.
- A beat using character-private evidence has exactly one NPC actor, and that
  actor must own every private binding on the beat.
- Ted may be a beat actor only through an exact current-user-source binding.
  The Python mechanical-connective allowance cannot authorize action,
  dialogue, thought, decision, movement, consent, or a new fact.
- Scene Summary v2 records one exact accepted-pair authority provenance entry
  for every source turn. Accepted-event hashes remain optional cross-checks.

### 2. Atomic acceptance recovery v2

- Accept and False Positive require the exact accepted user-message/final-
  sequence pair.
- One acceptance journal binds creator action, Validator package, exact pair
  and event paths/hashes, prior and prepared ACTIVE trees, the immutable
  promotion receipt payload, optional False Positive diagnostic, timeline,
  Planner-ledger state, and model-injection state.
- Recovery either restores the exact prior ACTIVE tree or completes every
  deterministic local acceptance artifact. Timeline and evidence writes are
  idempotent.
- A completed local acceptance with ambiguous or incomplete model-visible
  Planner injection remains explicitly pending and blocks continuation. CERA
  never silently replays the injection.
- Good uses ordinary Accept. Concern and Critical may use False Positive only
  when publication remains eligible; Python enforces the semantic-status and
  review-severity relationship.

### 3. Dispatch diagnostics and contract inventory v2

- Provider call events distinguish prepared, pretransport failure,
  transport-invoked, provider return/failure, post-validation failure, and
  typed acceptance. Only transport invocation consumes the call count.
- Continuous Codex Planner and Validator records require the exact stored-
  thread hash. Schema and MCP binding construction occurs before the call
  ledger enters provider dispatch.
- The live-canary harness wraps SDK import/capability loading, active-profile
  inspection, session materialization, and post-materialization role
  separation in named privacy-safe root-diagnostic stages.
- The provider-free Job 4 runner now imports directly from any working
  directory and explicitly closes SQLite before temporary cleanup.
- Character summary v2, evidence binding v2, call ledger v2, and Scene Summary
  derived view v2 are versioned. Persisted dataclass records are registered;
  mutable runtime journals are explicitly inventoried as dedicated operational
  formats rather than generic registry records.

## Verification boundary

Focused tests cover stale/wrong/fabricated summaries, package-bound derived
summaries, multi-actor private leakage, derived-only decisions, protected-user
allowances, Good/Concern/Critical review paths, every directory and local-
evidence crash cut, pending model injection, adapter-level pretransport and
post-invocation failures, stored-thread hashes, registry consistency, direct
runner startup, Scene Summary provenance, root diagnostics, and unchanged
D-180 identity.

Verification completed with:

- 79/79 focused continuous correction tests passing;
- 715/715 complete provider-free repository tests passing in 294.236 seconds;
- one expected environment-dependent symlink-creation skip;
- compileall, documentation validation, repository source inventory, active
  D-180 profile validation, direct Job 4 runner startup, and `git diff --check`
  passing.

Progressions 1-3 make zero provider calls, retries, fallbacks, story
database writes, active-route changes, service changes, installed SillyTavern
changes, deployments, merges, remotes, or pushes.

## Remaining boundary

The new correction-cycle-002 Stage 4 is a provider-free integration audit.
No live ten-call canary is authorized. Provider quality, latency, and live
transport behavior remain unproven.
