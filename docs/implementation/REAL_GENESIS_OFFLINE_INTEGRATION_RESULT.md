# Provider-Free Real-Genesis Integration and Retrieval Calibration Result

**Status:** accepted by ChatGPT Pro as provider-free offline integration  
**Date:** 2026-07-28  
**Repository:** `D:\AIChatBot\Cera`

## Authorized boundary

The creator authorized CERA to install the compiled Hanezawa Genesis into disposable development databases and exercise complete offline workflows with fake adapters. The authorization did not permit live providers, Adult EX import, SillyTavern integration, production story data, production-world binding, deployment, Phase 8 external-handler work, or Phase 9 derived-memory consolidation.

Every integration database is allocated inside an auto-deleting temporary directory. The harness does not accept a caller-selected database path, so it cannot be pointed at a production database accidentally.

## Outcome

The repository-local Hanezawa V1.1 package now installs into a disposable SQLite world and supports real evidence retrieval through the runtime contracts. The calibration exposed four material integration defects that synthetic fixtures had not revealed:

1. real Genesis records advertised fine-grained expandable fields while runtime evidence exposed only a wrapper payload;
2. the FTS5 candidate index omitted nested payload/section text, so indirect cues such as Hana's missed calls could not find the intended memory;
3. the combined household knowledge source section exposed unequal affair knowledge too broadly;
4. Reasoner validation enforced owner-private visibility but did not independently enforce `knowledge_owner_ids` at the character move, participation, and beat-use boundary.

The fixes preserve the immutable creator source files. Generated JSON authority, manifest, integrity binding, and views were rebuilt deterministically.

## Runtime corrections

- Real Genesis evidence now materializes every advertised field from top-level payload, grouped `fields`, or nested typed structures.
- Universal evidence sections remain available for `claim`, `knowledge`, `provenance`, `relationship`, and `route_flags`.
- Search candidates index authoritative nested payload/section text, while exact text still requires authorized fetch.
- The household knowledge model that juxtaposes objective truth and unequal knowledge is system-private; character-visible facts come from typed events, memories, beliefs, and relationships.
- Reasoner hard-evidence use now rechecks owner, subject, system-private, and `knowledge_owner_ids` for each acting character.
- `ReasonerSeedDossier.exact_seed_evidence` carries actual `ExactEvidence`, not metadata-only references. Seeded evidence can therefore support a cited decision without a duplicate Reasoner tool call.

## Real-package identity

- immutable core source SHA-256: `edf16b1204a0b8c656b0418e39b41c3306a995dc70a324c939b507483f783053`;
- immutable visual source SHA-256: `58cbb3231ce5d09434f830b57f430171ff69479848b0795f40f92a86407d6da9`;
- immutable prompt overlay SHA-256: `43680759edf68e57954bfc0c735525cfaba135f5c2594977a84f5804c7de12d4`;
- current manifest SHA-256: `99e070eb66e926fc38a53102db1fd1915ae06d672157d2672de9f83b9907c6dc`;
- current compiled bundle SHA-256: `f4663b67b868f454159026670575d2508deb9ea866d88564a629050477b5ae4b`;
- modules: 21;
- typed records: 505 total, 503 active after two explicit supersessions.

The former manifest `46e9d5b8...a9c4b` and bundle `68bb8597...9dcd` remain historical Phase 3 evidence but no longer identify the current compiled projection.

## Offline workflows exercised

- exact install and idempotent replay of the real package;
- every advertised real-Genesis section expanded through the runtime evidence adapter;
- indirect owner-memory lookup using `missed calls`, followed by selective exact expansion of Hana memory `H-M08`;
- exact privacy checks for affair/surveillance events `EN07`, `EN08`, and `ST06` across Hana, Mia, Sakura, and Enne perspectives;
- directional Hana-to-Ted relationship lookup without loading a whole profile;
- four bounded follow-up searches and deterministic failure of a fifth;
- one-character seeded reasoning using exact content with zero Reasoner tool calls;
- ordinary multi-character Hana/Mia Reasoner-to-Composer acceptance using fake adapters;
- rejection of Enne-only evidence as authority for a Hana decision;
- Hana's real adult-eligibility evidence through the consent-valid adult route, fake Reasoner, fake mechanics, and fake Composer;
- hard non-consensual blocker before any Reasoner, mechanics, Composer, or story write;
- SQLite append, exact replay, branch fork, sibling regeneration, restart, real Genesis binding recovery, and branch-local memory visibility.

The transaction rehearsal calls the existing storage contract directly. It proves storage semantics with real Genesis installed; it does not claim a live composer-to-publication coordinator exists.

## Validation evidence

- `python -m compileall -q src tests`: pass;
- complete repository suite: **154/154 pass** in 27.8 seconds;
- SQLite integrity check: `ok`;
- foreign-key check: clean;
- external provider calls: zero;
- Adult EX files imported: zero;
- persistent production databases created: zero;
- story artifacts committed outside disposable tests: zero.

## What this does not prove

This phase proves deterministic contracts and local integration. It does not qualify runtime Codex, DeepSeek, or any other provider; establish prose quality, psychological realism, latency, cost, or live reliability; create derived long-term memory consolidation; validate an external completion receipt; connect SillyTavern; or authorize deployment.

## Advisory review

ChatGPT Pro returned `REAL_GENESIS_OFFLINE_INTEGRATION_ACCEPTED`. Its acceptance is limited to deterministic, provider-free integration in auto-deleting development sandboxes. It explicitly does not qualify or authorize live providers, prose quality, publication coordination, Adult EX, SillyTavern, production-world binding, Phase 8, Phase 9, deployment, or persistent production databases.

Any next implementation work remains subject to the creator's separate authorization.
