# Continuous Short Canary V9 Specification

**State:** provider-free republication-readiness specification only; not authorized to execute

## Preserved live route

- Planner: `gpt-5.6-sol`, medium effort.
- Composer: `deepseek-v4-flash`, thinking disabled.
- Validator: `gpt-5.6-terra`, high effort.
- Three disposable turns across two scenes, exactly ten provider dispatches.
- One attempt per stage, no retry, fallback, hidden repair, substitution,
  eleventh call, or automatic False Positive.

## Required new identity

- Live-canary-001 and checkpoint
  `918b006f25ab638a7328287832796976158cdd3b` are preserved failure evidence and
  cannot be modified, retried, or reused.
- Any later execution requires a separately creator-authorized
  live-canary-002 checkpoint, cycle, task, authorization, and evidence set.
- The source snapshot must include the Cycle 009 canonical Job 4 result
  projection and accepted identity-bound Pro review.

## Result and terminal contract

- Every terminal live-shaped result must decode as the exact strict
  `cera.pro_review_job4_result.v1` shape and pass the real `complete-job4`
  transition.
- Authorization identity belongs to the manifest and receipt chain, not a
  duplicate top-level Job 4 field. Provider dispatches remain canonical
  effects; scripted transport counts are inapplicable to live execution.
- The canary stops on the first provider, schema, domain, semantic, privacy,
  branch, protected-user, persistence, publication, session, or cleanup
  failure. A submitted call always counts.

## Remaining live risks

Provider-free validation cannot establish live provider schema acceptance,
Validator semantic accuracy, story quality, thread continuity at provider
boundaries, latency, or token behavior. Only a new, separately authorized
live-canary-002 can measure those properties.
